{
  "name": "a-better-weather",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.mjs && node --check static/app.js",
    "test": "node --test test_worker.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 migrations apply a_better_weather --remote"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
